<h1><%= t('.new_lot') %></h1>

<% if @lot.errors.any? %>
  <p><%= t('.lot_error') %>:</p>
  <ul>
    <% @lot.errors.full_messages.each do |msg| %>
      <li>
        <%= msg %>
      </li>
    <%end%>
  </ul>
<%end%>

<%= render 'form_lot' %>